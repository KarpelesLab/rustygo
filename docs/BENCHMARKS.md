# Benchmarks

Generated by `go run ./internal/cmd/bench` on linux/amd64 (go1.26.5-X:nodwarf5, best of 5 runs).
Both builds are native; gc is `go build` with default flags, rustygo is
`cargo build --release` on the emitted crate. Runs of the two
binaries are interleaved so machine noise affects them alike.

## Run time

| benchmark | gc | rustygo | rustygo / gc | gc peak RSS | rustygo peak RSS |
|---|---:|---:|---:|---:|---:|
| calls | 114 ms | 65 ms | 0.56× | 2.6 MiB | 2.2 MiB |
| fields | 122 ms | 158 ms | 1.29× | 2.6 MiB | 2.4 MiB |
| pointers | 183 ms | 221 ms | 1.21× | 20.9 MiB | 63.3 MiB |
| scalar | 487 ms | 468 ms | 0.96× | 2.6 MiB | 2.2 MiB |
| strings | 199 ms | 873 ms | 4.40× | 10.4 MiB | 72.5 MiB |

## Compilation

Emitted lines count every generated `.rs` file. Build time is emit +
`cargo build --release` with the runtime crate already compiled.

| program | Go lines | emitted Rust lines | Rust lines per Go line | cargo build |
|---|---:|---:|---:|---:|
| calls | 11 | 62 | 5.6 | 389 ms |
| fields | 27 | 295 | 10.9 | 148 ms |
| pointers | 19 | 163 | 8.6 | 114 ms |
| scalar | 13 | 81 | 6.2 | 94 ms |
| strings | 21 | 126 | 6.0 | 110 ms |

## Compile-time scaling

Generated programs of the M0 subset, built from scratch (runtime crate
already compiled):

| functions | Go lines | emitted Rust lines | cargo build (release) | ms per 1k Go lines |
|---:|---:|---:|---:|---:|
| 250 | 6255 | 39331 | 4.58 s | 732 |
| 1000 | 25005 | 157081 | 25.00 s | 1000 |

The standard library of go1.26.5-X:nodwarf5 has 830154 non-blank lines of non-test Go
(GOROOT/src, excluding cmd/, testdata/ and _test.go). At the largest
program's rate that is about **830.02 s** of release build for the whole library.
This is linear extrapolation from generated code, so read it as an order of magnitude.
It is why M3 builds the stdlib once per Go version and target and caches it.
