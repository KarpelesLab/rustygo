# Programs that must keep passing the differential test (gc vs rustygo).
# The list only grows: a listed program that fails is a CI failure.
# A name followed by operating systems passes only there: the file layer
# reaches the kernel through Linux system calls for now (roadmap M5).
arith
arrays
bounds
consts
divzero
floatconv
floats
generics
globals
goto
hello
loops
multipkg
multiret
namedtypes
negshift
nilptr
panicint
panicnamed
panicstr
pointers
recursion
strbounds
strings
structs
switches
gcstress
slices
closures
defers
interfaces
recover
maps
interp
complex
unsafeptr
stdlib1
initdeps
reflectbasics
fmtbasics linux
stdlib2 linux
procexit linux
recoverrules
runtimeerrors
